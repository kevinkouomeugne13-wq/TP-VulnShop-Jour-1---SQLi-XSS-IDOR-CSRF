rules:
  - id: detect-raw-sql-concatenation
    pattern: db("SELECT ... " + ...)
    message: "🚨 Danger : Concaténation détectée dans une requête SQL ! Utilisez des requêtes paramétrées pour éviter les injections SQL."
    languages: [typescript, javascript]
    severity: ERROR
